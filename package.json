{
    "name": "tomasjonsgarden-portfolio",
    "version": "2.0.4",
    "private": true,
    "devDependencies": {
        "browser-sync": "^2.18.2"
    },
    "scripts": {
        "start": "browser-sync start --server --port=8080 --open=localhost:8080"
    }
}