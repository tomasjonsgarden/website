{
    "name": "tomasjonsgarden-project",
    "version": "2.0.4",
    "private": true,
    "devDependencies": {
        "browser-sync": "^2.18.2"
    },
    "scripts": {
        "start": "browser-sync start --open --server app"
    }
}